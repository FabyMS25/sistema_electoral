services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel-app
    restart: unless-stopped
    env_file:
      - .env.docker
    volumes:
      - .:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - db
    networks:
      - laravel-net
    ports:
      - "9000:9000"
    command: >
      sh -c "until pg_isready -h db -p 5432 -U postgres; do echo 'Waiting for Postgres...'; sleep 2; done &&
      php artisan migrate --seed --force &&
      /usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf"

  db:
    image: postgres:14
    container_name: postgres-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: counter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: counter
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5300:5432"
    networks:
      - laravel-net

volumes:
  postgres_data:

networks:
  laravel-net:
    driver: bridge